apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: auth
  name: auth
  namespace: dev
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: auth
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: auth
    spec:
      automountServiceAccountToken: true
      containers:
      - env:
        - name: WORKER
          value: "1"
        envFrom:
        - configMapRef:
            name: common-config
        - configMapRef:
            name: infra-config
        - secretRef:
            name: common-secret
        image: workforce2.azurecr.io/playground/auth:0.0.1
        imagePullPolicy: Always
        name: auth
        ports:
        - containerPort: 8001
          name: http
          protocol: TCP
        readinessProbe:
          failureThreshold: 3
          periodSeconds: 10
          successThreshold: 1
          tcpSocket:
            port: 8001
          timeoutSeconds: 1
        resources:
          limits:
            cpu: 300m
            memory: 512Mi
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
        volumeMounts:
        - mountPath: /mnt/data
          name: data
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      serviceAccount: dev-account
      serviceAccountName: dev-account
      terminationGracePeriodSeconds: 30
      volumes:
      - name: data
        emptyDir: {}