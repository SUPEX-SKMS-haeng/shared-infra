apiVersion: v1
kind: Service
metadata:
  name: auth-svc
  namespace: dev
spec:
  internalTrafficPolicy: Cluster
  ipFamilies:
  - IPv4
  ipFamilyPolicy: SingleStack
  ports:
  - port: 8001
    protocol: TCP
    targetPort: 8001
  selector:
    app: auth
  sessionAffinity: None
  type: ClusterIP